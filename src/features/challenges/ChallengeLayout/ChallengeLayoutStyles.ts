import { background, border } from "native-base/lib/typescript/theme/styled-system";
import { StyleSheet } from "react-native";

const challengeLayoutStyles = StyleSheet.create({
challengeTitle: {
    fontSize: 30,
},
});

export default challengeLayoutStyles;